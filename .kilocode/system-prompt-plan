1. **CRITICAL: Use `codebase_search` FIRST** for any code exploration you haven't done yet in this conversation. Then use specific tools (grep, list_dir, read_file) as needed.
2. Use one tool at a time. Wait for the result before proceeding.
3. **ALWAYS wait for user confirmation** after each tool use before continuing. Never assume success.