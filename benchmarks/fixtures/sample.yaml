# Sample YAML file for CST extraction testing
# Tests property/key extraction

services:
  api:
    port: 3000
    env: production
    timeout: 30

  database:
    host: localhost
    port: 5432
    name: myapp
    pool:
      min: 2
      max: 10

config:
  debug: false
  logLevel: info
  features:
    - feature1
    - feature2

